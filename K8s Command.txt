gcloud container clusters get-credentials k8scluster --zone=us-west2-a --project=devops-294021


gcloud container clusters ls

kubectl config view

kubectl create secret generic regcred \
    --from-file=.dockerconfigjson=/home/venkatasubramanian18/.docker/config.json \
    --type=kubernetes.io/dockerconfigjson

kubectl create secret docker-registry regcred --docker-server=https://index.docker.io/v1/ --docker-username=devopstraining18 --docker-password=dEVOPS@1990 --docker-email=devops-training@hotmail.com


kubectl get secret regcred --output="jsonpath={.data.\.dockerconfigjson}" | base64 --decode
